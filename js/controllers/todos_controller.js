Todos.TodosController = Ember.ArrayController.extend({	actions :{		createTodo: function(){			//Get the todo title set by the "New Todo" text field			var inputTitle = this.get('newTitle');			if(!inputTitle.trim())				return;			//Create the new Todo Model			var todoModel = this.store.createRecord('todo', {				title: inputTitle,				isCompleted: false			});			//Clear the "New Todo" text field			this.set('newTitle', '');			//Save the Todo Model			todo.save();		}	}});